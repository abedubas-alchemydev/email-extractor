[pytest]
testpaths = app/tests
pythonpath = .
asyncio_mode = auto
addopts = -m "not integration"
markers =
    integration: requires a reachable Postgres with migrations applied; skipped by default
filterwarnings =
    ignore::DeprecationWarning
