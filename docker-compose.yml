services:
  postgres:
    image: postgres:15-alpine
    container_name: email-extractor-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-email_extractor}
    ports:
      # Loopback bind: postgres is reachable from the docker bridge (backend → postgres:5432)
      # but NOT from the public internet. The VPS is shared/multi-tenant — never expose 5432.
      - "127.0.0.1:5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-email_extractor}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: email-extractor-backend
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-email_extractor}
      BACKEND_CORS_ORIGINS: ${BACKEND_CORS_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
      EMAIL_EXTRACTOR_API_KEY: ${EMAIL_EXTRACTOR_API_KEY:-}
      HUNTER_API_KEY: ${HUNTER_API_KEY:-}
      APOLLO_API_KEY: ${APOLLO_API_KEY:-}
      SNOV_API_KEY: ${SNOV_API_KEY:-}
      PORT: "8000"
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Empty -> the frontend uses same-origin /api/* paths. In Docker dev,
        # Next.js rewrites those to BACKEND_INTERNAL_URL below. On the VPS,
        # nginx terminates /api/* before it reaches the Next.js server.
        NEXT_PUBLIC_API_BASE_URL: ""
    container_name: email-extractor-frontend
    environment:
      NEXT_PUBLIC_API_BASE_URL: ""
      BACKEND_INTERNAL_URL: http://backend:8000
    ports:
      - "${FRONTEND_HOST_PORT:-3000}:3000"
    depends_on:
      - backend

volumes:
  pg_data:
    name: email-extractor-pg-data
